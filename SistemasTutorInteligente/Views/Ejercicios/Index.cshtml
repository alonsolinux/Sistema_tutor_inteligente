@model IEnumerable<SistemasTutorInteligente.Models.Ejercicio>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.NombreEjercicio)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Puntaje)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreEjercicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Puntaje)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IDejercicio">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IDejercicio">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IDejercicio">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
