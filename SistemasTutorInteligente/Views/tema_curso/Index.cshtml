@model IEnumerable<SistemasTutorInteligente.Models.tema_curso>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.nombre_tema)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombre_tema)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IDtemas">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IDtemas">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IDtemas">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
